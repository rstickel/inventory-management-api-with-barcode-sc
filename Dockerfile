FROM python:3.10-slim-busterW
FASKLINE _home /app_
ECH_dir -rc /src_
ECH_dir -rc /vavikeur
 COPY services.txt ._

RUN pip install -r requirements.txt

 COPY" .") /app/

code = await console.rename("src.main.app", "--host 0.0.0.0 --port 8000", defaults={})
  
    
